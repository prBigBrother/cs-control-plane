---
description: Ask a graph-first architecture, flow, dependency, or impact question.
---

Usage: `/knowledge [--scope auto|all|control-plane|repo] <question>`

Run `./.opencode/bin/knowledge-query $ARGUMENTS`. Auto guards control-plane questions from unrelated product nodes while preserving cross-repo product queries; pass an explicit scope when needed. Verify important graph claims with targeted source reads. Never extract or update from this command. Return flow, paths, runtime surfaces, risks, and open questions.
