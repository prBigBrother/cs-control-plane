/*
 * Placeholder plugin module for a self-hosted Mem0 deployment.
 * The control plane uses explicit tool scripts for memory writes so
 * the plugin surface can stay minimal until a live Mem0 endpoint is configured.
 */

export default {
  name: "mem0",
  description: "Self-hosted Mem0 integration placeholder for shared memory."
};
